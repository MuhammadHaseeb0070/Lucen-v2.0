---
phase: 01-foundation-finance-critical-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - supabase/functions/chat-proxy/index.ts
autonomous: true
requirements:
  - BUG-05
  - SEC-06
must_haves:
  truths:
    - "D-01: chat-proxy always assigns finishReason in every streaming exit path, and records Sentry warnings on null finishReason in billing"
    - "D-02: chat-proxy checks user status and rejects with 401 mid-stream if the token is expired"
    - "D-09: Document manual verification steps for non-automatable changes in a centralized VERIFICATION.md file"
  artifacts:
    - path: "supabase/functions/chat-proxy/index.ts"
      provides: "JWT expiration validation and billing integrity logic"
---

<objective>
Fix BUG-05 (finishReason billing drift) and SEC-06 (JWT expiry mid-stream) within the chat-proxy Edge function. Add Sentry breadcrumbs for billing anomalies and verify token status mid-turn.

Purpose: Prevent revenue loss and unauthorized model execution during long streams.
Output: Patched chat-proxy Edge function.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-finance-critical-fixes/01-CONTEXT.md
@.planning/phases/01-foundation-finance-critical-fixes/01-RESEARCH.md
@supabase/functions/chat-proxy/index.ts
</context>

<tasks>
<task type="auto">
  <name>Import Sentry and log breadcrumbs on null finishReason in chat-proxy</name>
  <files>supabase/functions/chat-proxy/index.ts</files>
  <read_first>supabase/functions/chat-proxy/index.ts</read_first>
  <action>Import Sentry from esm.sh at the top of supabase/functions/chat-proxy/index.ts. Initialize Sentry with the SENTRY_DSN env var if present. Identify the final billing/credit deduction block in chat-proxy and add a Sentry warning breadcrumb if finishReason is null (D-01). Ensure finishReason gets a default fallback or is handled cleanly on error/early exit paths so the accounting status is always correct.</action>
  <verify>grep -q "https://esm.sh/@sentry/deno" supabase/functions/chat-proxy/index.ts</verify>
  <acceptance_criteria>
    - Sentry is imported and initialized conditionally.
    - Sentry.addBreadcrumb is called when finishReason is null.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Implement mid-stream JWT expiration check and refresh verification</name>
  <files>supabase/functions/chat-proxy/index.ts</files>
  <read_first>supabase/functions/chat-proxy/index.ts</read_first>
  <action>In the SSE streaming loop in chat-proxy, check if the current timestamp is greater than or equal to the token's exp claim. If expired, call supabaseAdmin.auth.admin.getUserById(userId) to verify if the user exists and is active (D-02). If verification fails (e.g. user deleted or banned), emit a custom 401 error event: event: error, data: {"error": "Session expired. Please sign in again.", "code": 401} and terminate the stream by closing the controller.</action>
  <verify>grep -q "getUserById" supabase/functions/chat-proxy/index.ts</verify>
  <acceptance_criteria>
    - Edge function includes mid-stream expiry check relative to Deno.env/claims.
    - Edge function verifies user via admin client if token has expired mid-stream, aborting with 401 event if check fails.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] tsc -b passes compile checks
- [ ] Deno lint / compile checks pass on the modified edge function
- [ ] Document manual test steps in VERIFICATION.md (D-09)
</verification>

<success_criteria>
- chat-proxy compiles and handles both JWT verification and billing safeguards correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-finance-critical-fixes/01-02-SUMMARY.md`
</output>
