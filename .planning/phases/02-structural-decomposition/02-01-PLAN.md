---
phase: 02-structural-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/chat-proxy/index.ts
  - src/services/openrouter.ts
  - src/store/authStore.ts
  - src/store/chatStore.ts
  - src/store/themeStore.ts
  - src/store/uiStore.ts
  - src/store/creditsStore.ts
  - src/store/sideChatStore.ts
  - src/store/debugStore.ts
  - src/store/artifactStore.ts
  - src/lib/logger.ts
  - src/lib/artifactParser.ts
  - src/lib/iframeErrorBridge.ts
  - src/components/MessageBubble.tsx
autonomous: true
requirements:
  - TD-01
  - TD-02
  - TD-07
  - TD-08
  - TD-09
  - BUG-01
  - BUG-02
  - BUG-04
  - BUG-06
  - BUG-10
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
must_haves:
  truths:
    - "D-01: Monolith files openrouter.ts and chat-proxy are decomposed using index facade exports"
    - "D-02: Zustand stores are decoupled via a centralized src/store/orchestration.ts module using subscribeWithSelector"
    - "D-03: Correlation IDs are generated on the client and forwarded to backend edge functions via X-Correlation-ID headers"
    - "D-04: Theme fingerprint calculations are optimized using color change deep comparisons, and MessageBubble utilizes React.memo"
  artifacts:
    - path: "src/store/orchestration.ts"
      provides: "Zustand store orchestrator tracking cross-store events and subscriptions"
    - path: "supabase/functions/chat-proxy/auth.ts"
      provides: "JWT extraction and user checks for chat-proxy"
    - path: "supabase/functions/chat-proxy/billing.ts"
      provides: "Billing calculation and credit deduction for chat-proxy"
    - path: "supabase/functions/chat-proxy/streamHandler.ts"
      provides: "Stream forwarding and tool execution loops for chat-proxy"
    - path: "supabase/functions/chat-proxy/utils.ts"
      provides: "Output validation and utility helpers for chat-proxy"
    - path: "src/services/openrouter/messages.ts"
      provides: "Message compiling and pruning logic"
    - path: "src/services/openrouter/rag.ts"
      provides: "RAG vector retrieval service calls"
    - path: "src/services/openrouter/streaming.ts"
      provides: "SSE parsing and text cleaning helpers"
    - path: "src/services/openrouter/continuation.ts"
      provides: "Auto-continuation loops and token budget calculations"
    - path: "src/services/openrouter/client.ts"
      provides: "Main streamChat fetch calls and wrapper orchestration"
  key_links:
    - from: "src/services/openrouter.ts"
      to: "src/services/openrouter/client.ts"
      via: "re-export facade"
    - from: "supabase/functions/chat-proxy/index.ts"
      to: "supabase/functions/chat-proxy/streamHandler.ts"
      via: "import and delegate serve request"
---

<objective>
Decompose openrouter.ts and chat-proxy/index.ts monoliths into small modular files with a clean facade entry point. Unify all direct cross-store imports with a centralized Zustand orchestration layer. Standardize logging with correlation IDs passed via request headers, enforce state versioning migrations, fix identified bugs, and apply targeted rendering and slider optimizations.

Purpose: Improve codebase maintainability, isolate store states, enable searchable request traces, and fix critical layout and rendering bugs.
Output: Monolith splits, store migrations, orchestration module, logging headers, performance improvements, and bug fixes.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structural-decomposition/02-CONTEXT.md
@.planning/phases/02-structural-decomposition/02-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Decompose Deno chat-proxy monolith and fix BUG-06</name>
  <files>supabase/functions/chat-proxy/index.ts, supabase/functions/chat-proxy/auth.ts, supabase/functions/chat-proxy/billing.ts, supabase/functions/chat-proxy/streamHandler.ts, supabase/functions/chat-proxy/utils.ts</files>
  <read_first>supabase/functions/chat-proxy/index.ts</read_first>
  <action>Create auth.ts, billing.ts, streamHandler.ts, and utils.ts in supabase/functions/chat-proxy/. Split index.ts into these files, housing the main loop, jwt logic, billing logic, and validation utils. In streamHandler.ts, ensure the try-catch wrapper always writes data: [DONE]\n\n on errors or watchdog triggers to fix BUG-06. Rewrite index.ts to act as the thin entry point importing and invoking the sub-modules.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - supabase/functions/chat-proxy/ contains auth.ts, billing.ts, streamHandler.ts, and utils.ts.
    - index.ts does not exceed 300 lines and delegates successfully.
    - Deno imports are correct and relative.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Decompose openrouter.ts client service, fix BUG-01, and lazy load continuation</name>
  <files>src/services/openrouter.ts, src/services/openrouter/messages.ts, src/services/openrouter/rag.ts, src/services/openrouter/streaming.ts, src/services/openrouter/continuation.ts, src/services/openrouter/client.ts</files>
  <read_first>src/services/openrouter.ts</read_first>
  <action>Create messages.ts, rag.ts, streaming.ts, continuation.ts, and client.ts in src/services/openrouter/. Move message pruning/compiling, RAG fetches, SSE parsing, and main streamChat code out of the monolith. In streaming.ts, wrap the processStream loop in try-finally to ensure onDone is always called after onError (BUG-01). Use dynamic import() in client.ts to lazy-load continuation module functions (PERF-04). Rewrite src/services/openrouter.ts to re-export the public interface of client.ts.</action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - src/services/openrouter/ sub-modules exist.
    - openrouter.ts re-exports the required streamChat and type definitions facade.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Implement centralized Zustand store orchestration and versioning (TD-08, TD-09, BUG-10)</name>
  <files>src/store/orchestration.ts, src/store/authStore.ts, src/store/chatStore.ts, src/store/themeStore.ts, src/store/uiStore.ts, src/store/creditsStore.ts, src/store/sideChatStore.ts, src/store/artifactStore.ts</files>
  <read_first>src/store/authStore.ts</read_first>
  <action>Create src/store/orchestration.ts. Set up subscriptions to authStore, themeStore, and chatStore using subscribeWithSelector to handle cross-store triggers (e.g. signout triggers chat/theme sync cleanups). Remove direct getState() imports between store files. Add incremental version fields and migrate functions to the 4 persisted stores (chat, ui, credits, sideChat) to audit out legacy PII. In artifactStore.ts, reset worker and log state when focusedArtifactId changes (BUG-10).</action>
  <verify>npx tsc --noEmit && npm run test</verify>
  <acceptance_criteria>
    - src/store/orchestration.ts exists and initializes subscriptions on startup.
    - Stores do not import each other directly.
    - Persisted stores specify version and migrate handlers.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Implement correlation logging and UI optimizations (TD-07, BUG-02, BUG-04, PERF-01, PERF-02, PERF-03)</name>
  <files>src/lib/logger.ts, src/services/openrouter/client.ts, src/lib/artifactParser.ts, src/lib/iframeErrorBridge.ts, src/components/MessageBubble.tsx, src/store/debugStore.ts, src/store/themeStore.ts</files>
  <read_first>src/lib/logger.ts</read_first>
  <action>Modify client.ts to generate correlation IDs and pass them via X-Correlation-ID headers to edge functions. Update src/lib/logger.ts to accept and log correlation IDs in structured format. Replace 50+ raw console.* calls with logger calls (TD-07). In artifactParser.ts, strip orphaned closing tags (BUG-02). In iframeErrorBridge.ts, correct injectIntoHtml head matching regex (BUG-04). Memoize theme fingerprinting in themeStore.ts to avoid 60Hz JSON.stringify calls (PERF-01). Wrap MessageBubble in React.memo (PERF-02). Evict old debug logs in debugStore.ts (PERF-03).</action>
  <verify>npx tsc --noEmit && npm run test</verify>
  <acceptance_criteria>
    - Correlation IDs are passed in headers.
    - console.* calls replaced.
    - Theme slider runs smoothly.
    - MessageBubble renders memoized.
    - All tests pass.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] tsc -b compiles successfully (zero TypeScript issues)
- [ ] npm run test unit tests pass with coverage gates met
- [ ] npm run e2e Playwright smoke test passes
- [ ] Manual test verifies theme slider drag is smooth and doesn't freeze browser
</verification>

<success_criteria>
- Both monoliths decomposed into clean, small sub-modules with facade entry points.
- Cross-store Zustand imports completely removed and replaced by orchestration.ts.
- Tracing correlation IDs successfully flowing from client headers to server edge functions.
- All unit and E2E tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-structural-decomposition/02-01-SUMMARY.md`
</output>
