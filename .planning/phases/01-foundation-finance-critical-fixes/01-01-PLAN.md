---
phase: 01-foundation-finance-critical-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.app.json
  - playwright.config.ts
  - tests/e2e/smoke.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "D-04: npm run test executes Vitest extending vite.config.ts and reports V8 code coverage"
    - "D-05: Coverage thresholds are configured and enforced as CI gates"
    - "D-07: npm run e2e executes Playwright E2E tests under tests/e2e on Chromium only"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright configuration file"
    - path: "tests/e2e/smoke.test.ts"
      provides: "Playwright smoke test checking page load and basic title assertion"
  key_links:
    - from: "package.json"
      to: "vite.config.ts"
      via: "test script running vitest"
---

<objective>
Configure the test runner infrastructure, including Vitest for unit tests (extending vite.config.ts) and Playwright for E2E testing (Chromium only). Configure coverage thresholds and write an initial Playwright E2E smoke test.

Purpose: Build a testing foundation to prevent regressions during monolith decomposition and bug fixing.
Output: Configured packages, test commands, configurations, and a smoke test.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-finance-critical-fixes/01-CONTEXT.md
@.planning/phases/01-foundation-finance-critical-fixes/01-RESEARCH.md
@package.json
@vite.config.ts
@tsconfig.app.json
</context>

<tasks>
<task type="auto">
  <name>Install test dependencies and add test scripts</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>Install vitest, @vitest/coverage-v8, jsdom, and @playwright/test as devDependencies. Add "test": "vitest run --coverage" and "e2e": "playwright test" scripts to package.json.</action>
  <verify>npm run test -- --help && npx playwright --version</verify>
  <acceptance_criteria>
    - package.json contains "vitest", "@vitest/coverage-v8", "jsdom", and "@playwright/test" in devDependencies.
    - package.json contains "test" and "e2e" scripts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Configure Vitest in vite.config.ts</name>
  <files>vite.config.ts, tsconfig.app.json</files>
  <read_first>vite.config.ts, tsconfig.app.json</read_first>
  <action>Add /// <reference types="vitest" /> at the top of vite.config.ts. Extend defineConfig to include a 'test' property configuring globals: true, environment: 'jsdom', and coverage provider: 'v8' with thresholds (D-04, D-05). Update tsconfig.app.json to include "vitest/globals" in compilerOptions.types if needed.</action>
  <verify>npx vitest --version</verify>
  <acceptance_criteria>
    - vite.config.ts contains a test configuration block matching the specified thresholds.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Configure Playwright and write E2E smoke test</name>
  <files>playwright.config.ts, tests/e2e/smoke.test.ts</files>
  <read_first>playwright.config.ts</read_first>
  <action>Create playwright.config.ts configured to run tests inside 'tests/e2e/' directory, targeting Chromium browser only, with localhost dev server details (D-07). Create tests/e2e/smoke.test.ts to verify the main layout, check if title is 'Lucen' or elements are present.</action>
  <verify>npx playwright test --help</verify>
  <acceptance_criteria>
    - playwright.config.ts exists and specifies Chromium browser only.
    - tests/e2e/smoke.test.ts exists and performs a basic assertion.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] npm install completes without conflicts
- [ ] tsc -b compiles successfully
- [ ] npm run test runs successfully (though it may report no tests found yet)
- [ ] npx playwright test runs smoke test against dev server or mock endpoint if dev environment isn't run locally
</verification>

<success_criteria>
- Test dependencies installed and config files successfully created.
- vitest and playwright configurations verify properly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-finance-critical-fixes/01-01-SUMMARY.md`
</output>
