---
phase: 05-csp-enforce-e2e-final-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - src/main.tsx
  - src/lib/logger.ts
  - src/components/ArtifactRenderer.tsx
  - src/lib/logger.test.ts
  - src/components/ArtifactRenderer.test.tsx
autonomous: true
requirements:
  - SEC-02
  - PROD-01
  - PROD-06
  - BUG-06
must_haves:
  truths:
    - "vercel.json has Content-Security-Policy-Report-Only headers configured matching index.html requirements."
    - "Sentry is initialized in src/main.tsx with build-time release version and environment."
    - "src/lib/logger.ts routes leveled logs (info, warn, error) automatically to Sentry breadcrumbs, and redacts PII."
    - "Iframe sandbox attribute is restricted to 'allow-scripts' only, and a sandbox capability notice is rendered when disallowed features are detected in the HTML."
  artifacts:
    - path: "vercel.json"
      provides: "Security headers configuration with CSP Report-Only rules"
    - path: "src/lib/logger.ts"
      provides: "Sentry integrated leveled logging and PII redaction"
    - path: "src/components/ArtifactRenderer.tsx"
      provides: "Sandboxed preview iframe and UX notice rendering"
  key_links:
    - from: "src/lib/logger.ts"
      to: "@sentry/react"
      via: "Sentry.addBreadcrumb"
---

<objective>
Configure CSP Report-Only headers, wire Sentry telemetry with release/environment versioning, integrate logger logs into Sentry breadcrumbs with PII redaction, tighten the iframe sandbox permissions, and render warning notices for sandbox-restricted artifacts.

Purpose: Solidify production security headers and wire up global error/telemetry monitoring.
Output: CSP headers, unified Sentry logger integration, and tightened sandbox UI warnings.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-csp-enforce-e2e-final-verification/05-CONTEXT.md
@vercel.json
@src/main.tsx
@src/lib/logger.ts
@src/components/ArtifactRenderer.tsx
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add CSP Report-Only headers in vercel.json</name>
  <read_first>
    - file:///E:/Lucen/Lucen-v2.3 fresh/vercel.json
  </read_first>
  <action>
    Edit vercel.json to add the 'Content-Security-Policy-Report-Only' header under headers.
    Set the header value to match index.html restrictions:
    "default-src 'self'; script-src 'self' 'unsafe-eval' https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com; img-src 'self' data: blob: https://*.supabase.co https://*.supabase.in; font-src 'self' data: https://fonts.gstatic.com https://cdn.jsdelivr.net; connect-src 'self' https://*.supabase.co https://*.supabase.in https://openrouter.ai https://api.tavily.com wss://*.supabase.co wss://*.supabase.in; frame-src 'self' blob:; worker-src 'self' blob:; object-src 'none'; base-uri 'self'; form-action 'self';"
  </action>
  <acceptance_criteria>
    - vercel.json contains Content-Security-Policy-Report-Only in headers config.
  </acceptance_criteria>
  <done>
    CSP Report-Only header is added to vercel.json.
  </done>
</task>

<task type="execute">
  <name>Task 2: Configure Sentry release/environment and unify breadcrumbs in logger.ts</name>
  <read_first>
    - file:///E:/Lucen/Lucen-v2.3 fresh/src/main.tsx
    - file:///E:/Lucen/Lucen-v2.3 fresh/src/lib/logger.ts
    - file:///E:/Lucen/Lucen-v2.3 fresh/src/lib/logger.test.ts
  </read_first>
  <action>
    In src/main.tsx, import the package.json version via Vite define/injection (e.g. define APP_VERSION in vite.config.ts if needed, or import from package.json or metadata) and assign it to the 'release' property in Sentry.init (e.g. `release: "lucen@" + (import.meta.env.VITE_APP_VERSION || "0.0.0")`).
    In src/lib/logger.ts, import * as Sentry from '@sentry/react' and update the log functions (debug, info, warn, error) to call Sentry.addBreadcrumb with corresponding categories and severity levels (info, warning, error).
    Ensure the log content filters out user prompts, responses, or passwords (PII redaction) before sending the message to Sentry.
    Update src/lib/logger.test.ts to verify Sentry breadcrumb integration is called correctly.
  </action>
  <acceptance_criteria>
    - src/main.tsx initializes Sentry with a release version config.
    - src/lib/logger.ts imports Sentry and calls Sentry.addBreadcrumb for warning/error logs.
    - npm test runs all tests successfully.
  </acceptance_criteria>
  <done>
    Sentry is configured with release versioning and linked directly to the unified logger.
  </done>
</task>

<task type="execute">
  <name>Task 3: Tighten iframe sandbox and add warning notices</name>
  <read_first>
    - file:///E:/Lucen/Lucen-v2.3 fresh/src/components/ArtifactRenderer.tsx
    - file:///E:/Lucen/Lucen-v2.3 fresh/src/components/ArtifactRenderer.test.tsx
  </read_first>
  <action>
    In src/components/ArtifactRenderer.tsx:HtmlRenderer, update the iframe's sandbox attribute to be "allow-scripts" only (removing any allow-forms, allow-popups, allow-modals, etc.).
    Add code to scan the generated HTML previewContent (using DOMParser or a simple regex) for elements that require sandbox-disabled features (e.g. `<form>`, `<input>`, `window.open()`, `alert()`).
    If disallowed elements are detected, display a styled amber/warning banner notice at the top of the preview pane header explaining the sandbox restrictions.
    Update src/components/ArtifactRenderer.test.tsx to assert this behavior.
  </action>
  <acceptance_criteria>
    - sandbox attribute in HtmlRenderer iframe is exactly "allow-scripts".
    - A sandbox warning banner is shown if forms or modals are present in the HTML artifact code.
    - Vitest unit tests pass.
  </acceptance_criteria>
  <done>
    Iframe sandbox is restricted to allow-scripts and a sandbox UX banner is rendered when disallowed features are detected.
  </done>
</task>

<task type="execute">
  <name>Task 4: Verify chat-proxy [DONE] sentinel on edge error</name>
  <read_first>
    - file:///E:/Lucen/Lucen-v2.3 fresh/supabase/functions/chat-proxy/streamHandler.ts
  </read_first>
  <action>
    Inspect supabase/functions/chat-proxy/streamHandler.ts to verify that in all catch/finally blocks, the `[DONE]` sentinel is emitted using `controller.enqueue(encoder.encode("data: [DONE]\n\n"))` and that the stream is cleanly closed.
    If any path misses the sentinel, add it.
  </action>
  <acceptance_criteria>
    - catch block in streamHandler.ts handles enqueueing [DONE] sentinel.
    - finally block in streamHandler.ts guarantees controller.close() is called.
  </acceptance_criteria>
  <done>
    Chat-proxy is verified to cleanly close streams on error.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Sentry | Telemetry events sent from user browser to Sentry endpoints. Must protect user PII. |
| Host Page → Artifact Iframe | Rendered HTML artifacts within the preview pane. Tightened sandbox prevents clickjacking/phishing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Sentry SDK | mitigate | Redact message payloads and user metadata from logger telemetry. |
| T-05-02 | Elevation of Privilege | Artifact Iframe | mitigate | Restrict sandbox to allow-scripts only (removes allow-forms, allow-popups, allow-modals). |
| T-05-03 | Denial of Service | chat-proxy | mitigate | Ensure chat-proxy emits [DONE] on internal error to break client retry continuation loops. |
</threat_model>

<verification>
npm run test
</verification>

<success_criteria>
CSP headers are added to vercel.json. Sentry release version and logger integration are wired. Sandbox permissions are restricted and warnings render. Unit tests pass.
</success_criteria>

<output>
Create .planning/phases/05-csp-enforce-e2e-final-verification/05-01-SUMMARY.md when done
</output>
