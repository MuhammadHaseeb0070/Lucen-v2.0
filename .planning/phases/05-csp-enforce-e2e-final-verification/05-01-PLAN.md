---
phase: 05-csp-enforce-e2e-final-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - vite.config.ts
  - src/main.tsx
  - src/lib/logger.ts
  - src/store/creditsStore.ts
  - src/store/authStore.ts
  - src/services/openrouter/client.ts
  - src/components/ArtifactRenderer.tsx
  - tests/e2e/core.test.ts
autonomous: true
requirements:
  - SEC-02
  - PROD-01
  - PROD-05
  - PROD-06
  - BUG-06
must_haves:
  truths:
    - "D-01: CSP configured as Report-Only in vercel.json first"
    - "D-02: Static CSP configuration with allowlist of trusted domains and no dynamic nonces"
    - "D-05: Sentry breadcrumbs integrated into unified src/lib/logger.ts"
    - "D-06: Sentry release configured dynamically using package.json version via Vite define"
    - "D-07: Credit/billing failures capture exceptions and send to Sentry with PII redacted"
    - "D-09: Intercept all backend APIs using Playwright page.route in E2E tests"
    - "D-13: Tighten iframe sandbox to allow-scripts only"
    - "D-14: Iframe sandbox notice banner rendered when blocked interactive elements are detected"
  artifacts:
    - path: "tests/e2e/core.test.ts"
      provides: "E2E tests covering 6 core flows: login, message, file, artifact, buy credits, subscription"
    - path: "src/components/ArtifactRenderer.test.tsx"
      provides: "Unit tests asserting iframe sandbox warning banner visibility"
  key_links:
    - from: "src/lib/logger.ts"
      to: "src/main.tsx"
      via: "Sentry integration where logs automatically record breadcrumbs"
---

<objective>
Configure Content-Security-Policy (CSP) headers in vercel.json, wire Sentry telemetry with automated logger breadcrumbs and release versioning, tighten the preview iframe sandbox to allow-scripts with a warning notice banner, fix/verify that the Deno chat-proxy edge function returns a [DONE] sentinel on internal errors, and write Playwright E2E integration tests targeting the 6 core client-side user flows.

Purpose: Harden production security, wire full error instrumentation, and establish browser-based E2E regressions coverage.
Output: Production-grade headers, Sentry configuration, tightened iframe React UI, and a complete E2E test suite.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-csp-enforce-e2e-final-verification/05-CONTEXT.md
@.planning/phases/05-csp-enforce-e2e-final-verification/05-RESEARCH.md
@.planning/phases/05-csp-enforce-e2e-final-verification/05-VALIDATION.md
@vercel.json
@vite.config.ts
@src/main.tsx
@src/lib/logger.ts
@src/components/ArtifactRenderer.tsx
</context>

<tasks>
<task type="auto">
  <name>Configure CSP Report-Only headers in vercel.json</name>
  <files>vercel.json</files>
  <read_first>vercel.json</read_first>
  <action>Add "Content-Security-Policy-Report-Only" header to the headers section of vercel.json (SEC-02). Restrict script-src to 'self' and https://cdn.jsdelivr.net. Restrict connect-src to 'self' and Supabase URLs, openrouter.ai, api.tavily.com, and sentry ingest. Restrict style-src to 'self' 'unsafe-inline' (for style tags and Google Fonts). Do not add a report-uri directive (D-04).</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - vercel.json contains a Content-Security-Policy-Report-Only header key.
    - The CSP string allows only verified domains: 'self', supabase.co/supabase.in, openrouter.ai, api.tavily.com, sentry.io/ingest.sentry.io, fonts.googleapis.com/fonts.gstatic.com, and cdn.jsdelivr.net.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Inject APP_VERSION at build time and configure Sentry release</name>
  <files>vite.config.ts, src/main.tsx</files>
  <read_first>vite.config.ts, src/main.tsx, package.json</read_first>
  <action>Add a 'define' block in vite.config.ts to inject APP_VERSION from package.json version: APP_VERSION: JSON.stringify(process.env.npm_package_version || '0.0.0') (D-06). Update src/main.tsx Sentry.init config to include release: APP_VERSION and verify environment mapping.</action>
  <verify>npm run build && npm run test</verify>
  <acceptance_criteria>
    - vite.config.ts defines a global build-time variable for version string.
    - src/main.tsx uses this version string to initialize the Sentry release tag.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Route central logger warning/error events to Sentry breadcrumbs</name>
  <files>src/lib/logger.ts</files>
  <read_first>src/lib/logger.ts</read_first>
  <action>Import Sentry from '@sentry/react' dynamically or check if initialized before calling. Integrate Sentry.addBreadcrumb in logger.info, logger.warn, and logger.error (D-05). Redact all message content (prompts, replies), credentials, or bearer tokens to prevent PII leakage in Sentry (D-08).</action>
  <verify>npx vitest run src/lib/logger.test.ts</verify>
  <acceptance_criteria>
    - src/lib/logger.ts has import Sentry from '@sentry/react'.
    - logger warn/error/info logs trigger Sentry.addBreadcrumb.
    - Test suite passes confirming Sentry logger integration works without crashing.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Capture exceptions for billing, auth, and stream failures</name>
  <files>src/store/creditsStore.ts, src/store/authStore.ts, src/services/openrouter/client.ts</files>
  <read_first>src/store/creditsStore.ts, src/store/authStore.ts, src/services/openrouter/client.ts</read_first>
  <action>Wrap billing (credit deduction), auth initialization/failures, and chat streams in try/catch blocks that invoke Sentry.captureException(err) when failures occur. Redact specific user PII before sending (D-07, D-08).</action>
  <verify>npm run test</verify>
  <acceptance_criteria>
    - Sentry.captureException is invoked on database or network failures in creditsStore.ts and authStore.ts.
    - Sentry.captureException is invoked on network failure in openrouter client.ts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tighten iframe sandbox and render warning banner inside ArtifactRenderer</name>
  <files>src/components/ArtifactRenderer.tsx, src/components/ArtifactRenderer.test.tsx</files>
  <read_first>src/components/ArtifactRenderer.tsx</read_first>
  <action>Confirm iframe sandbox is strictly allow-scripts (remove allow-forms, allow-popups, allow-modals) (D-13). In HtmlRenderer, scan previewContent for disallowed HTML tags/methods: form, input, textarea, button type=submit, window.open, alert(, confirm(, prompt(. If found, display a visible amber warning banner notice at the top of preview pane warning the user that interactive forms or popups are disabled (D-14). Write unit tests in src/components/ArtifactRenderer.test.tsx confirming warning notices render correctly.</action>
  <verify>npx vitest run src/components/ArtifactRenderer.test.tsx</verify>
  <acceptance_criteria>
    - Iframe sandbox is strictly sandbox="allow-scripts".
    - HtmlRenderer detects blocked interactive elements and displays a styled warning notice banner.
    - ArtifactRenderer.test.tsx verifies warning banner rendering.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify Deno chat-proxy edge function writes [DONE] sentinel on errors</name>
  <files>supabase/functions/chat-proxy/streamHandler.ts</files>
  <read_first>supabase/functions/chat-proxy/streamHandler.ts</read_first>
  <action>Verify and assert that the try/catch in streamHandler.ts always emits data: [DONE]\n\n on internal errors to terminate the stream client-side (BUG-06).</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Deno streamHandler.ts writes [DONE] sentinel before closing stream controller on error paths.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Write Playwright E2E integration tests for 6 core flows</name>
  <files>tests/e2e/core.test.ts</files>
  <read_first>tests/e2e/smoke.test.ts</read_first>
  <action>Create tests/e2e/core.test.ts. Use page.route to intercept and mock Supabase Auth (/auth/v1/token, /auth/v1/user) and Edge Functions (/functions/v1/chat-proxy, /functions/v1/get-file-content, etc.) (D-09). Implement 6 tests: (1) sign-in with mock credentials (D-10), (2) sending message and verifying stream chunking, (3) attaching files with mocked processor text, (4) rendering HTML artifacts, (5) buying credits simulating checkout callbacks (D-11), (6) viewing subscription states (D-12).</action>
  <verify>npm run e2e</verify>
  <acceptance_criteria>
    - tests/e2e/core.test.ts compiles and executes successfully.
    - All 6 tests pass without requiring real external service keys.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- [ ] npm run build compiles the project cleanly (`tsc -b && vite build`)
- [ ] npm run test runs all Vitest tests successfully with 50%+ coverage
- [ ] npm run e2e runs both smoke.test.ts and core.test.ts successfully
</verification>

<success_criteria>
- CSP Report-Only configured in vercel.json.
- Sentry wired with release, env, and logger breadcrumbs.
- Sandbox warning notice correctly rendered when forms/popups exist in artifact code.
- Playwright E2E suite passes fully with mock backend APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-csp-enforce-e2e-final-verification/05-01-SUMMARY.md`
</output>
