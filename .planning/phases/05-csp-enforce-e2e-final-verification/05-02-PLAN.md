---
phase: 05-csp-enforce-e2e-final-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01-PLAN.md
files_modified:
  - tests/e2e/core.test.ts
autonomous: true
requirements:
  - PROD-05
must_haves:
  truths:
    - "tests/e2e/core.test.ts exists and runs successfully under Playwright."
    - "API requests (Supabase Auth and Edge Functions) are mocked via page.route in tests."
    - "Playwright E2E test suite covers: signing in, sending messages, attaching files, running artifacts, and purchasing credits."
  artifacts:
    - path: "tests/e2e/core.test.ts"
      provides: "Automated E2E tests for the 6 core user workflows"
  key_links:
    - from: "tests/e2e/core.test.ts"
      to: "playwright"
      via: "@playwright/test framework"
---

<objective>
Write a comprehensive E2E test suite in tests/e2e/core.test.ts covering sign-in, message sending, file attachments, artifact running, and credit checkouts, mocking all network dependencies via Playwright page.route.

Purpose: Provide automated regression testing for the core Lucen application flows.
Output: E2E Playwright test script covering all 6 critical user flows.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-csp-enforce-e2e-final-verification/05-CONTEXT.md
@tests/e2e/smoke.test.ts
@playwright.config.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: Scaffold E2E test suite and mock login</name>
  <read_first>
    - file:///E:/Lucen/Lucen-v2.3 fresh/tests/e2e/smoke.test.ts
  </read_first>
  <action>
    Create tests/e2e/core.test.ts.
    Scaffold the tests with imports from '@playwright/test'.
    Use `test.beforeEach` to intercept Supabase Auth routes (e.g. `**/auth/v1/token*`, `**/auth/v1/user*`) using `page.route` to return mock user payloads (user ID 'local-user', email 'test-e2e@lucen.app').
    Write the first test block covering the User Sign-In flow: navigate to login screen, fill email/password inputs, hit submit, and assert user dashboard/chat interface loads.
  </action>
  <acceptance_criteria>
    - tests/e2e/core.test.ts is created.
    - Test executes login flow and page header is visible.
    - npx playwright test tests/e2e/core.test.ts passes.
  </acceptance_criteria>
  <done>
    E2E test suite is scaffolded and the login flow test is implemented.
  </done>
</task>

<task type="execute">
  <name>Task 2: Implement E2E tests for sending messages and running artifacts</name>
  <read_first>
    - file:///E:/Lucen/Lucen-v2.3 fresh/tests/e2e/core.test.ts
  </read_first>
  <action>
    In tests/e2e/core.test.ts, add a test for sending a message.
    Mock the chat-proxy endpoint (`**/functions/v1/chat-proxy`) using `page.route` to stream back mock SSE text fragments (e.g. content start, delta chunks, and a trailing [DONE] sentinel).
    Simulate user typing in the chat input and clicking Send. Verify that the streamed response is rendered inside a message bubble.
    Add a test for running artifacts. Trigger a mock response containing `<lucen_artifact id="test-id" type="html">...</lucen_artifact>` tags and assert that the preview iframe loads and contains the generated output.
  </action>
  <acceptance_criteria>
    - Chat message sending test runs and asserts message text.
    - Artifact execution test runs and asserts iframe renders.
    - npx playwright test tests/e2e/core.test.ts passes.
  </acceptance_criteria>
  <done>
    E2E tests for streaming chat messages and rendering HTML artifacts are implemented.
  </done>
</task>

<task type="execute">
  <name>Task 3: Implement E2E tests for file attachments and credit purchases</name>
  <read_first>
    - file:///E:/Lucen/Lucen-v2.3 fresh/tests/e2e/core.test.ts
  </read_first>
  <action>
    In tests/e2e/core.test.ts, add a test for attaching files. Mock the upload and process-file APIs to return a mock file card in the chat input.
    Add a test for purchasing credits and viewing subscription status. Mock the Lemon Squeezy payment modal trigger and simulate a successful payment trigger callback in Zustand/state, verifying that user credits increment in the sidebar UI.
  </action>
  <acceptance_criteria>
    - File attachment test compiles and passes.
    - Credit purchase test verifies balance change.
    - npx playwright test tests/e2e/core.test.ts is fully green.
  </acceptance_criteria>
  <done>
    E2E tests for attaching files and mock credit purchasing are implemented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A (Testing environment)

## STRIDE Threat Register
N/A (Testing environment)
</threat_model>

<verification>
npx playwright test tests/e2e/core.test.ts
</verification>

<success_criteria>
tests/e2e/core.test.ts covers sign-in, message streaming, artifact execution, file attachment, and mock credit updates. Playwright test suite passes.
</success_criteria>

<output>
Create .planning/phases/05-csp-enforce-e2e-final-verification/05-02-SUMMARY.md when done
</output>
