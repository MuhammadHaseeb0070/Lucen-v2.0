---
phase: 03-billing-hardening-production-shared-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/themes.ts
  - src/store/uiStore.ts
  - supabase/functions/_shared/circuitBreaker.ts
  - supabase/functions/_shared/rateLimit.ts
  - supabase/functions/chat-proxy/index.ts
  - supabase/functions/chat-proxy/streamHandler.ts
  - supabase/functions/classify-intent/index.ts
  - supabase/functions/deduct-credits/index.ts
  - supabase/functions/describe-image/index.ts
  - supabase/functions/embed/index.ts
  - supabase/functions/generate-title/index.ts
  - supabase/functions/get-file-content/index.ts
  - supabase/functions/get-model-config/index.ts
  - supabase/functions/ls-checkout/index.ts
  - supabase/functions/ls-webhook/index.ts
  - supabase/functions/retrieve-chunks/index.ts
  - supabase/functions/web-search/index.ts
autonomous: true
requirements:
  - TD-03
  - TD-04
  - TD-05
  - SEC-04
  - PROD-02
  - PROD-03
  - PROD-04
  - PERF-05
must_haves:
  truths:
    - "All 11 edge functions enforce rate limiting and kill switches at entry."
    - "Rate limiter and circuit breaker use Upstash Redis shared state with TTL expiry in production, falling back to local in-memory states in dev."
    - "Web search flag is unified to web_search_enabled, warning on legacy keys."
    - "Themes are extracted to src/config/themes.ts and setViewerFile is properly typed."
  artifacts:
    - path: "src/config/themes.ts"
      provides: "Extracted theme configurations and color palettes"
    - path: "supabase/functions/_shared/rateLimit.ts"
      provides: "Redis-backed rate limiting with local fallback"
    - path: "supabase/functions/_shared/circuitBreaker.ts"
      provides: "Redis-backed circuit breaker with local fallback"
  key_links:
    - from: "supabase/functions/chat-proxy/index.ts"
      to: "supabase/functions/_shared/rateLimit.ts"
      via: "checkRateLimit invocation"
---

<objective>
Harden the billing and gateway layer of Lucen. Configure rate limiting and kill switch checks on all edge function endpoints, migrate the OpenRouter circuit breaker and rate limiting states to Upstash Redis for multi-isolate persistence, unify the web search flag keys, extract the theme configuration configurations, and type the ViewerFile interface.

Purpose: Prevent API abuse, ensure circuit-breaker resilience across edge isolates, standardize flags, and clean up store typing.
Output: Upstash Redis connection layer, rate limiters, circuit breakers, unified keys, themes config, and clean types.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-billing-hardening-production-shared-state/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Redis-backed Rate Limiter and Circuit Breaker with Local Fallbacks</name>
  <files>supabase/functions/_shared/rateLimit.ts, supabase/functions/_shared/circuitBreaker.ts</files>
  <action>
    Modify supabase/functions/_shared/rateLimit.ts to support Upstash Redis connection via REST APIs (using UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN).
    Provide in-memory sliding-window local fallbacks if Redis credentials are missing.
    In supabase/functions/_shared/circuitBreaker.ts, migrate the OpenRouter circuit breaker state storage to Upstash Redis with a 30-second TTL.
  </action>
  <verify>
    npm run test passes.
  </verify>
  <done>
    Shared Redis state is enabled for rate limiter and circuit breaker with dev-local fallbacks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce Rate Limiting and Kill Switches across Edge Functions</name>
  <files>supabase/functions/chat-proxy/index.ts, supabase/functions/classify-intent/index.ts, supabase/functions/deduct-credits/index.ts, supabase/functions/describe-image/index.ts, supabase/functions/embed/index.ts, supabase/functions/generate-title/index.ts, supabase/functions/get-file-content/index.ts, supabase/functions/get-model-config/index.ts, supabase/functions/ls-checkout/index.ts, supabase/functions/ls-webhook/index.ts, supabase/functions/retrieve-chunks/index.ts, supabase/functions/web-search/index.ts</files>
  <action>
    Enforce checkRateLimit and isKillSwitched checks at the entry point of all 11 edge functions.
    Respond with 429 Too Many Requests (with Retry-After header) or 503 Service Unavailable when triggers match.
  </action>
  <verify>
    Edge functions compile successfully.
  </verify>
  <done>
    All edge functions check rate limits and kill switches on requests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Unify Web Search Flag, Extract Themes, and Type ViewerFile</name>
  <files>src/config/themes.ts, src/store/uiStore.ts, supabase/functions/chat-proxy/streamHandler.ts</files>
  <action>
    Unify search keys in request handlers to web_search_enabled, emitting a deprecation warning on legacy keys.
    Extract themes out of src/store/uiStore.ts to src/config/themes.ts.
    Strongly type setViewerFile and ViewerFile interface in src/store/uiStore.ts.
  </action>
  <verify>
    npm run build completes without compiler errors.
  </verify>
  <done>
    Search flags are unified, themes are isolated, and uiStore is strongly typed.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | Edge Gateways | mitigate | Implement Redis rate limiting with 429 responses and Retry-After header. |
| T-03-02 | Denial of Service | OpenRouter API | mitigate | Circuit breaker halts traffic and degrades UI gracefully when downstream is down. |
</threat_model>

<verification>
npm run test
</verification>

<success_criteria>
All edge functions are rate-limited and kill-switched. Circuit breaker states are shared. Themes are extracted. Typing is clean.
</success_criteria>

<output>
Create .planning/phases/03-billing-hardening-production-shared-state/03-01-SUMMARY.md when done
</output>
