---
wave: 1
depends_on: []
files_modified:
  - src/config/prompts.ts
  - .planning/phases/15-patch-format-parser/15-UAT.md
autonomous: true
---

# Phase 15-02: Patch Format Verification Gap Closure

## Objective
Fix issues discovered during UAT:
1. Instruct the AI model to output Git conflict markers for patches when modifying existing artifacts, rather than regenerating the full artifact content.
2. Update the UAT test for the HTML Sanity Check to reflect that the artifact viewer is read-only.

## Must Haves
- The system prompt in `src/config/prompts.ts` must contain strict instructions for using `<<<<<<< SEARCH`, `=======`, `>>>>>>> REPLACE` markers when updating an artifact.
- Test 2 in `15-UAT.md` must be rewritten to ask the AI to generate broken HTML rather than asking the user to manually edit the code.

## Tasks

<task>
  <id>15-02-01</id>
  <title>Update system prompt for strict patch format enforcement</title>
  <read_first>
    - src/config/prompts.ts
  </read_first>
  <action>
    - Update `BASE_SYSTEM_PROMPT` inside `src/config/prompts.ts` in the `<artifacts>` section.
    - Add explicit instructions: "When the user asks to modify an existing artifact, DO NOT regenerate the entire artifact. You MUST use Git conflict markers to patch it."
    - Provide an example of the patch format using `<<<<<<< SEARCH`, `=======`, `>>>>>>> REPLACE`.
  </action>
  <acceptance_criteria>
    - `src/config/prompts.ts` contains `<<<<<<< SEARCH` and `=======` and instructions to use Git conflict markers.
  </acceptance_criteria>
</task>

<task>
  <id>15-02-02</id>
  <title>Update UAT Test 2 to remove manual editing instruction</title>
  <read_first>
    - .planning/phases/15-patch-format-parser/15-UAT.md
  </read_first>
  <action>
    - Modify the `expected` field for Test 2 ("HTML Sanity Check") in `15-UAT.md`.
    - Change it from "Manually edit the patch..." to "Ask the AI to generate a patch that intentionally breaks HTML syntax (e.g., leaving a tag unclosed). The UI should surface an HTML sanity check failure error rather than corrupting the preview."
  </action>
  <acceptance_criteria>
    - `15-UAT.md` no longer contains the phrase "Manually edit the patch".
  </acceptance_criteria>
</task>

## Verification
- Prompt the AI to modify an artifact and ensure it outputs Git conflict markers instead of regenerating the artifact.
- Run the modified UAT Test 2 by asking the AI to generate broken HTML and confirming the sanity check error triggers.

## Artifacts this phase produces
- None (modifies existing prompt and test files).
